apiVersion: v1
kind: Service
metadata:
  name: orders
  labels:
    app: orders
spec:
  type: {{ .Values.service.type }}
  selector:
    app: orders
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP