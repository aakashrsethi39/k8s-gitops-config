apiVersion: v1
kind: Service
metadata:
  name: users
  labels:
    app: users
spec:
  type: {{ .Values.service.type }}
  selector:
    app: users
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80